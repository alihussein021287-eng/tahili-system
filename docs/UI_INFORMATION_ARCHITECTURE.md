# UI Information Architecture

هذا الملف مولد من الكود بواسطة `node scripts/audit-project.mjs --markdown`. لا يحتوي بيانات تشغيلية.

## Summary

- generatedFrom: repository source only
- pages: 93
- layouts: 2
- loadingFiles: 1
- errorFiles: 1
- notFoundFiles: 1
- apiRoutes: 18
- actionFiles: 36
- exportedServerActions: 256
- componentFiles: 44
- libFiles: 58
- prismaModels: 87
- prismaEnums: 61
- permissions: 139
- roles: 15
- tests: 58
- migrations: 16
- unclassifiedPages: 0

## Page Classification

| route | الوحدة | الصلاحية | hub/tab | البيانات / Actions | النوع والحكم | حدود التعديل | legacy/تكرار | العرض والثيم والحالات | الخطر |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/` | العمل اليومي | analytics.view, appointments.view, approvals.view, attendance.view, audit.view | / / من query `tab` | admission, appointment, auditLog, careStage, centerMembership, device; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/account` | الوصول والحساب | حراسة server/session أو route parent | مستقلة / - | lib/API/props أو بلا بيانات; changeOwnPassword | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/analytics` | التقارير والمالية | analytics.view, pharmacy.view | /reports-finance / analytics | admission, appointment, branch, center, device, governorate; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/appointments` | المرضى والرعاية | appointments.create, appointments.delete, appointments.edit, appointments.view | /patients-care / appointments | appointment, branch, center, centerMembership, patient; createAppointment, deleteAppointment, rescheduleAppointment, setAppointmentStatus, updateAppointmentCenterHall | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/appointments/[id]/qr` | المرضى والرعاية | appointments.view | /patients-care / - | appointment; قراءة فقط/route handler | طباعة 🖨️ ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/appointments/calendar` | المرضى والرعاية | appointments.view | /patients-care / calendar | appointment; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/approvals` | التقارير والمالية | approvals.approve, approvals.create, approvals.execute, approvals.review, approvals.view | /reports-finance / approvals | approvalRequest, patient; approveApproval, createApproval, executeApproval, rejectApproval, reviewApproval | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/attendance` | الموظفون والإدارة | attendance.view | /staff / attendance | lib/API/props أو بلا بيانات; قراءة فقط/route handler | إدارية 🔁 🧭 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | /staff?tab=attendance؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/audit` | النظام | حراسة server/session أو route parent | /settings / audit | auditLog; قراءة فقط/route handler | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/backup` | النظام | حراسة server/session أو route parent | /settings / backup | orgSetting; createBackup, deleteBackup, restoreBackupAction, testBackupIntegrity, toggleAutoBackup | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/beds` | العلاج والمراكز | beds.assign, beds.manage, beds.view | /therapy-centers / beds | admission, room; addBed, addRoom, assignBed, deleteRoom, updateRoom | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/care-board` | المرضى والرعاية | journey.view | /patients-care / care-board | careStage; قراءة فقط/route handler | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/centers` | العلاج والمراكز | centers.view | /therapy-centers / centers | center; قراءة فقط/route handler | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/centers/[slug]` | العلاج والمراكز | centers.memberships.manage, centers.programs.manage, centers.psych.sensitive | /therapy-centers / - | centerMembership, centerProgram, referralRequest, user; قراءة فقط/route handler | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/centers/[slug]/programs/[id]` | العلاج والمراكز | centers.programs.finalize, centers.programs.manage, centers.psych.sensitive | /therapy-centers / - | centerProgram, centerResource; finalizeCenterProgram, scheduleCenterSession | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/centers/[slug]/resources` | العلاج والمراكز | centers.resources.manage | /therapy-centers / - | centerResource, centerSession, room, therapyHall; saveCenterResource, setCenterResourceStatus | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/centers/[slug]/today` | العلاج والمراكز | centers.psych.sensitive, centers.sessions.record | /therapy-centers / - | centerSession; recordCenterSession | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/centers/reports` | العلاج والمراكز | centers.view | /therapy-centers / reports | center, centerProgram, centerSession; قراءة فقط/route handler | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/collaboration` | التعاون | chat.create, chat.manage.members, chat.moderate, chat.send, collaboration.view | /collaboration / من query `tab` | center, conversation, user; قراءة فقط/route handler | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/collaboration/admin` | التعاون | files.admin | /collaboration / admin | center, user; createChannelAction, rescanFileAction, saveCollaborationSettingsAction, transferOwnerAction | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/collaboration/files` | التعاون | files.admin, files.delete, files.delete.permanent, files.download, files.edit | /collaboration / files | center, folder, patient, user; قراءة فقط/route handler | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/devices` | أخرى | devices.create, devices.delete, devices.maintain, devices.view, tasks.create | مستقلة / - | device, patient; createDevice, createMaintenanceTask, deleteDevice, markReplaced, recordMaintenance | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/display` | واجهات مستقلة | حراسة server/session أو route parent | مستقلة / - | lib/API/props أو بلا بيانات; قراءة فقط/route handler | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` | منخفض |
| `/finance` | التقارير والمالية | expenses.view, finance.delete, finance.invoice, finance.payment, finance.receipt | /reports-finance / finance | invoice, patient; createInvoice, deleteInvoice, payFull, recordPayment, updateInvoiceDate | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/[id]/receipt` | التقارير والمالية | finance.receipt | /reports-finance / - | invoice; updateInvoiceDate | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/expenses` | التقارير والمالية | expenses.amounts, expenses.create, expenses.view | /reports-finance / expenses | patient, woundedExpense; createExpense | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/expenses/[id]` | التقارير والمالية | expenses.amounts, expenses.approve, expenses.correct, expenses.create, expenses.pay | /reports-finance / - | patient, woundedExpense; cancelExpense, createExpenseCorrection, decideExpense, markExpenseReady, payExpense, submitExpense | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/expenses/[id]/voucher` | التقارير والمالية | expenses.amounts, expenses.print | /reports-finance / - | woundedExpense; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/expenses/reports` | التقارير والمالية | expenses.amounts, expenses.reports | /reports-finance / reports | patient, woundedExpense; قراءة فقط/route handler | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/payments` | التقارير والمالية | finance.view | /reports-finance / payments | patient, payment; قراءة فقط/route handler | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/finance/report` | التقارير والمالية | finance.report | /reports-finance / report | invoice; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/inventory` | الصيدلية والمخزون | inventory.manage, inventory.view | /pharmacy-inventory / inventory | medication; adjustStock, createMedication, setStockSettings | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/login` | الوصول والحساب | حراسة server/session أو route parent | مستقلة / - | lib/API/props أو بلا بيانات; activateAccount, checkUsername | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; محلي; `app/error.tsx` | منخفض |
| `/login-log` | النظام | حراسة server/session أو route parent | /settings / login-log | loginLog; قراءة فقط/route handler | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; محلي; `app/error.tsx` | متوسط |
| `/maintenance` | النظام | حراسة server/session أو route parent | /settings / maintenance | admission, appointment, attendance, auditLog, center, correspondence; wipeAll, wipeCategory | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/meds` | العلاج والمراكز | meds.manage, meds.view | /therapy-centers / meds | admission, medDose; addMedDose, deleteMedDose, toggleMedGiven | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/my-work` | العمل اليومي | appointments.view, approvals.view, dashboard.view, expenses.approve, expenses.pay | / / my-work | appointment, approvalRequest, careStage, center, medicationBatch, prescription; قراءة فقط/route handler | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/notifications` | التنبيهات | حراسة server/session أو route parent | /notifications / من query `tab` | notification; markAllNotificationsRead, markNotificationRead | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/official-docs` | التقارير والمالية | officialdocs.manage, officialdocs.view | /reports-finance / official-docs | officialDocument, patient; addOfficialDocument | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/official-docs/[id]` | التقارير والمالية | officialdocs.manage, officialdocs.view | /reports-finance / - | officialDocument, patient; updateOfficialDocument | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/patients` | المرضى والرعاية | patients.create, patients.export, patients.import, patients.view | /patients-care / patients | branch, patient; قراءة فقط/route handler | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients-care` | المرضى والرعاية | appointments.view, journey.view, patients.create, patients.export, patients.import | /patients-care / من query `tab` | appointment, branch, careStage, district, governorate, injuryType; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/patients/[id]` | المرضى والرعاية | beds.view, centers.central.view, clinical.admission, clinical.session, clinical.view | /patients-care / - | centerMembership, patient, referralRequest, reportApproval, therapyHall, user; archivePatient, createTask, generatePortalToken, restorePatient, revokePortalToken | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/admission/[admissionId]` | المرضى والرعاية | clinical.admission | /patients-care / - | admission; قراءة فقط/route handler | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/card` | المرضى والرعاية | patients.print | /patients-care / - | patient; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/care-print` | المرضى والرعاية | clinical.care, clinical.view | /patients-care / - | dressingRecord, medication, patient; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/edit` | المرضى والرعاية | patients.edit | /patients-care / - | patient; action, updatePatient | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/journey-print` | المرضى والرعاية | journey.view | /patients-care / - | patient; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/medical-report/[reportId]` | المرضى والرعاية | reports.print | /patients-care / - | medicalReport; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/report` | المرضى والرعاية | patients.print, reports.approve | /patients-care / - | patient, reportApproval; approvePatientReport, unapprovePatientReport | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/sick-leave/[lid]` | المرضى والرعاية | sickleave.view | /patients-care / - | patient, reportApproval, sickLeave, user; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/[id]/sick-leave/[lid]/official` | المرضى والرعاية | sickleave.view | /patients-care / - | orgSetting, patient, reportApproval, sickLeave, therapySession, user; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/data-quality` | المرضى والرعاية | patients.view | /patients-care / data-quality | lib/API/props أو بلا بيانات; قراءة فقط/route handler | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/import` | المرضى والرعاية | patients.import | /patients-care / import | lib/API/props أو بلا بيانات; قراءة فقط/route handler | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/patients/new` | المرضى والرعاية | patients.create | /patients-care / new | lib/API/props أو بلا بيانات; createPatient | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/permissions` | النظام | حراسة server/session أو route parent | /settings / permissions | auditLog, rolePermission, user, userPermission; قراءة فقط/route handler | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/pharmacy` | الصيدلية والمخزون | pharmacy.dispense, pharmacy.dispense.partial, pharmacy.print, pharmacy.view | /pharmacy-inventory / pharmacy | medication, medicationBatch, prescription; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy-inventory` | الصيدلية والمخزون | inventory.view, pharmacy.batch, pharmacy.dispense, pharmacy.purchase.create, pharmacy.purchase.prices | /pharmacy-inventory / من query `tab` | medication, medicationBatch, prescription, purchaseOrder, purchaseOrderItem, purchaseReceipt; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/log` | الصيدلية والمخزون | pharmacy.view | /pharmacy-inventory / log | stockMovement; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/patient/[id]` | الصيدلية والمخزون | pharmacy.print, pharmacy.view | /pharmacy-inventory / - | patient, prescription; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/purchases` | الصيدلية والمخزون | pharmacy.purchase.create, pharmacy.purchase.view | /pharmacy-inventory / purchases | purchaseOrder, supplier; createPurchaseOrder | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/purchases/[id]` | الصيدلية والمخزون | pharmacy.purchase.approve, pharmacy.purchase.create, pharmacy.purchase.override, pharmacy.purchase.prices, pharmacy.purchase.receive | /pharmacy-inventory / - | medication, purchaseOrder; addPurchaseItem, receivePurchaseItem, transitionPurchaseOrder | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/purchases/reports` | الصيدلية والمخزون | pharmacy.purchase.prices, pharmacy.purchase.view | /pharmacy-inventory / reports | medication, purchaseOrder, purchaseOrderItem, purchaseReceipt; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/reports` | الصيدلية والمخزون | pharmacy.view | /pharmacy-inventory / reports | medication, medicationBatch, stockMovement; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/rx/[id]` | الصيدلية والمخزون | pharmacy.print, pharmacy.view | /pharmacy-inventory / - | prescription; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/pharmacy/stock` | الصيدلية والمخزون | pharmacy.batch, pharmacy.view | /pharmacy-inventory / stock | medication, supplier; addSupplier, createMedication, deleteSupplier | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** قواعد الأهلية والمخزون وFEFO والصرف الذري | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/portal/[token]` | واجهات مستقلة | حراسة server/session أو route parent | مستقلة / - | patient; respondAppointment | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` | متوسط |
| `/queue` | المرضى والرعاية | queue.manage, queue.view | /patients-care / queue | center, patient, queueEntry; addToQueue, clearDoneQueue, removeQueue, setQueueStatus | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/readiness` | النظام | audit.view, settings.backup, settings.view | /settings / readiness | admission, auditLog, branch, center, device, leave; قراءة فقط/route handler | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/referrals` | المرضى والرعاية | referrals.view | /patients-care / referrals | referralRequest; قراءة فقط/route handler | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** آلة حالات الإحالة، الأدوار، المستندات والانتقالات | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/referrals/[id]` | المرضى والرعاية | referrals.accept, referrals.cancel, referrals.print, referrals.recordResult, referrals.reviewResult | /patients-care / - | auditLog, center, referralRequest, user; transitionReferralAction, updateReferralAction | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** آلة حالات الإحالة، الأدوار، المستندات والانتقالات | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/referrals/[id]/official` | المرضى والرعاية | referrals.print | /patients-care / - | referralRequest; قراءة فقط/route handler | طباعة 🖨️ ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** آلة حالات الإحالة، الأدوار، المستندات والانتقالات | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/reports` | التقارير والمالية | patients.export, reports.print, reports.view | /reports-finance / reports | admission, appointment, governorate, patient, therapySession; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/reports-finance` | التقارير والمالية | analytics.view, approvals.view, audit.view, clinical.report, expenses.amounts | /reports-finance / من query `tab` | approvalRequest, auditLog, invoice, medicalReport, officialDocument, patient; قراءة فقط/route handler | مالية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** المبالغ، الموافقات، القيود، الحالات وServer Actions | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/reports/daily` | التقارير والمالية | devices.view, pharmacy.view, reports.print, reports.view | /reports-finance / daily | admission, appointment, branch, device, medication, medicationBatch; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/reports/official` | التقارير والمالية | reports.approve, reports.official, reports.print | /reports-finance / official | admission, device, invoice, patient, reportApproval, therapySession; approveOfficialReport, unapproveOfficialReport | طباعة 🖨️ ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/reports/statistical` | التقارير والمالية | reports.approve, reports.print, reports.view | /reports-finance / statistical | admission, approvalRequest, branch, device, governorate, injuryType; approveStatReport, unapproveStatReport | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/search` | المرضى والرعاية | appointments.view, devices.view, officialdocs.view, patients.view, pharmacy.view | /patients-care / search | appointment, branch, center, device, officialDocument, patient; receptionCheckIn | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/settings` | النظام | settings.backup, settings.edit, settings.view | /settings / من query `tab` | branch, center, centerResource, displayDevice, formation, governorate; action, addBranch, addCenterHall, addDistrict, addFormation, addInjuryType | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/setup` | الوصول والحساب | حراسة server/session أو route parent | مستقلة / - | orgSetting, user; completeSetup | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/shifts` | الموظفون والإدارة | shifts.view | /staff / shifts\|leaves | lib/API/props أو بلا بيانات; قراءة فقط/route handler | إدارية 🔁 🧭 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | /staff?tab=shifts\|leaves؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/staff` | الموظفون والإدارة | attendance.manage, attendance.view, shifts.approve, shifts.manage, shifts.view | /staff / من query `tab` | attendance, branch, employee, leave, patient, shift; addShift, checkIn, checkOut, completeTask, createTask, deleteAttendance | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/station-kpis` | المرضى والرعاية | journey.view, queue.view, reports.view, tasks.view | /patients-care / station-kpis | careStage; قراءة فقط/route handler | إدارية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/tasks` | الموظفون والإدارة | tasks.view | /staff / tasks | lib/API/props أو بلا بيانات; قراءة فقط/route handler | إدارية 🔁 🧭 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | /staff?tab=tasks؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: خاص؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/therapy` | العلاج والمراكز | therapy.view | /therapy-centers / therapy | centerMembership, referralRequest, treatmentPlan, user; قراءة فقط/route handler | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/therapy-centers` | العلاج والمراكز | beds.view, centers.central.view, centers.sessions.record, centers.view, clinical.plan | /therapy-centers / من query `tab` | admission, appointment, center, centerAssessment, centerMembership, centerProgram; قراءة فقط/route handler | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ - | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/therapy/plans/[id]/final` | العلاج والمراكز | therapy.plan.finalize | /therapy-centers / - | centerMembership, treatmentPlan; finalizeTherapyPlan | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/therapy/today` | العلاج والمراكز | therapy.session.record | /therapy-centers / today | appointment; recordTherapyAppointment | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الخطة والجلسة والعضوية والرقود وانتقالاتها | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | عال |
| `/users` | الموظفون والإدارة | حراسة server/session أو route parent | /staff / users | branch, user; createUser | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/users/[id]` | الموظفون والإدارة | حراسة server/session أو route parent | /staff / - | auditLog, branch, loginLog, user, userPermission; قراءة فقط/route handler | إدارية حساسة 🔒 ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الصلاحيات، المصادقة، التدقيق والنسخ | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: ضمن الغلاف/تحقق؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ ضمنية/تحتاج تحقق; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/visits` | المرضى والرعاية | queue.manage, visits.manage, visits.view | /patients-care / visits | center, patient, visit; receptionCheckIn, recordVisit | طبية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** الحقول الطبية والسجل السريري وحالات الرعاية | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | متوسط |
| `/workload` | العلاج والمراكز | clinical.session, workload.view | /therapy-centers / workload | appointment, centerMembership, therapySession; قراءة فقط/route handler | علاجية ⚠️ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
| `/workspaces` | العمل اليومي | حراسة server/session أو route parent | / / workspaces | lib/API/props أو بلا بيانات; قراءة فقط/route handler | تشغيلية ✅ | يمكن تعديل العرض فقط مع مقارنة السلوك؛ **ممنوع:** السلوك والroute والـdeep links والصلاحيات القائمة | -؛ يعرض أيضاً عبر hub/تبويب؛ لا تحذف route | responsive: نعم؛ DS: مشترك/جزئي؛ يرث theme؛ راجع الألوان الخاصة؛ موجودة; `(app)/loading.tsx`; `app/error.tsx` + not-found | منخفض |
